# 灵犀 Canvas 交互规则（原型实施版）

> 本文是灵犀原型中 Canvas 选区交互的实施规范，描述当前有效的交互定义、呈现规则与设计约束。

## 1. Canvas 要解决什么问题

用户在灵犀里让 AI 生成或打开文件后，不应该只能用一句笼统的话继续追问。用户应能直接在文件里指出"这里"，再说明"怎么改"。

Canvas 把对话和文件连起来：

```text
用户打开文件 → 在文件里选中局部 → 说明希望怎么处理 → 灵犀修改 → 用户在原文件里查看结果
```

这里的"局部"可以是一段文字、演示页上的一段文字，或表格中的一个连续单元格区域。

## 2. 打开和查看文件

### 2.1 从对话打开产物

灵犀在对话中生成文档、演示或表格后，以文件卡片展示。用户点击文件卡片，即在右侧 Canvas 打开该文件。

- 可以同时打开多个文件，并通过顶部标签切换。
- 右侧文件区呈现为内嵌的 Office 编辑环境：文件名、保存/分享操作、编辑工具栏，以及文件内容本身。
- 用户可以关闭文件标签。关闭后如还有其他已打开文件，则显示相邻文件；没有文件时收起右侧文件区。

### 2.2 用户始终可以手动编辑

Canvas 不是只读预览。文档正文、演示页文字形状、表格单元格都允许用户直接修改：

- 文字：正文整篇可置光标、打字、换行、删除。
- 演示：左侧缩略图切换页面；页内各文字形状（眉题、标题、副标题、内容条目、页脚、演讲者备注）可编辑。
- 表格：点击单元格进入编辑，回车或失焦提交；有限行列内滚动查看。

## 3. 选择局部内容

### 3.1 用户如何选择

- 文字、演示：像平常一样拖动鼠标选中文字（演示限于文字形状内）。
- 表格：拖动选择一个连续的单元格区域；双击单元格直接修改内容。

选中后，选区附近出现一个悬浮工具栏。

悬浮工具栏的来源：Figma 稿中无此节点，按产品侧确认的线上截图还原布局；容器采用 KDesign Popover 规范（白底、padding 8px、1px 描边、大阴影、圆角 middle），栏内按钮采用工具栏 Medium 28px 规格，图标取自 KDicon-pro。布局为：AI 编辑（灵犀 icon，首位）｜字体｜字号｜加粗 斜体｜字体色 填充色｜对齐 列表｜左右收起箭头（展示用）。

### 3.2 工具栏规则

- 工具栏第一个按钮是 **AI 编辑**，带灵犀 icon；用户一眼就能知道可以对当前选区发起局部修改。
- 其余按钮（加粗、斜体、字号等）按线上悬浮工具栏还原，作为 Office 编辑环境的展示；本 Demo 不要求它们可用。
- 用户点击文件空白处、文件其他区域或工具栏外部时，选区工具栏消失。
- 用户点击 AI 编辑后出现的输入框，在输入文字、点击添加或发送时必须保持打开，不能因为点击事件被误关掉。

## 4. 用户发起局部 AI 编辑

点击 **AI 编辑** 后，用户看到一个小输入框：上方告诉用户当前选中了什么（呈现方式与选区标签一致），下方输入希望 AI 如何处理。

例如：

> 改得更有说服力，但不要增加篇幅。

用户有两种选择：**发送**，或**添加到输入框**。

### 4.1 发送：立刻处理这一处

适用于用户已经想清楚，只需要改这一处。

用户输入指令后点击"发送"（或按 Enter）：

1. 对话中立即出现一条用户消息。
2. 消息里清楚展示：引用的是哪个文件、哪个选区，以及用户的局部指令。
3. 该选区退出待编辑状态，悬浮工具栏与输入框关闭，用户进入"灵犀正在处理"的反馈流程。

选区标签与用户指令是两类信息：标签负责说明"改哪里"，指令负责说明"怎么改"。界面上不能把用户指令伪装成标签的一部分。

### 4.2 添加到输入框：组织一轮更完整的修改

适用于用户想先收集多个要改的地方，并在发送前组织一整段话。

用户输入局部指令后点击"添加到对话"（或按 Ctrl/Cmd + Enter）：

1. 将这个选区作为一个标签插入左侧主输入框中。
2. 在标签后紧接着放入该选区的局部指令，为可编辑普通文字，用户可继续修改它。

待发送选区**不在文件侧留下任何标记**：Canvas 内不加黄色高亮，不出现批注卡片。

用户可以在同一个主输入框里自由组合：

```text
先统一整体语气，[选区 A] 改得更精炼；然后 [选区 B] 补充一个渠道动作，最后保持总篇幅不变。
```

三条不可妥协的规则：

- 选区标签是一个整体：用户不能进入标签内部改字，只能整体删除。
- 用户自己的文字与选区标签可以任意穿插，光标可放在标签前后继续输入。
- 每个选区的局部指令是可编辑文字，不是标签的一部分。用户可以把光标放进这段指令中间，重新措辞或补充要求。

如果用户删除一个选区标签：取消该选区的待发送状态；原本已经写下的局部指令保留为普通文字，避免用户辛苦输入的内容丢失。

### 4.3 选区标签的呈现

- 输入框内与已发送消息中的选区标签，统一展示为：**组件 icon + 选区类型 + 选区内容前 12 字**（超出以 … 截断）。
  - 选区类型文案：统一为"选区"二字（不区分文字/演示/表格），类型差异由组件 icon 表达。
  - 表格选区的内容取选区内非空单元格文本按行优先拼接。
- hover 选区标签时展示：**组件 icon + 完整文件名 + 完整选区内容**；选区内容最多 200 字，超出以 … 截断；表格选区额外展示区域地址（如 B4:D7）。
- 标签样式参照现有"输入框内引用文件 pill"的设计语言，按 KDesign 规范设计为新组件。

## 5. 发送混排后的主输入框

当用户点击主输入框的发送按钮：

- 对话中按用户原本的顺序展示普通文字和各个选区标签。
- 普通文字不应被包进任一选区标签；多个标签之间的文字不能丢失或调换顺序。
- 发送后主输入框内容清空，本轮待发送选区全部解除。
- 如果这条消息带了一个或多个选区，灵犀开始处理这些选区。

如果用户只输入普通文字而没有选区，它就是一条普通对话消息，不需要显示"正在编辑文件"的状态。

## 6. 灵犀编辑中的反馈

用户发送带选区的请求后，系统应让用户明确感知到：灵犀正在改的是哪份文件，且当前内容可能即将变化。

### 6.1 发送后立即

- 左侧：灵犀回复"好的，收到，正在编辑中。"
- 左下主输入框：进入生成态，暂时不可再次发送或继续添加选区。
- 右侧：仅受本轮影响的文件顶部出现黄色提示条（样式按 KDesign Message/Banner warning 规范：底色 #FEF1EA、描边 #E2651A、橙三角警示图标）：

> 本文档正在被灵犀编辑中，手动修改的内容可能丢失。

提示的含义是风险告知，不是锁文件。用户仍可手动编辑文件内容。

### 6.2 修改完成并刷新预览

本 Demo 使用固定时间模拟这个过程：约 10 秒后，灵犀回复"好的，已修改完成。"；受影响文件进入"正在更新预览"，约 2.5 秒后恢复正常展示，黄色提示条消失，主输入框恢复可用。

真实产品中，这两个节点应由真实 AI 任务和文件更新结果驱动，而不是固定倒计时。

### 6.3 多文件的可见性

用户可以在等待期间切换到其他已打开文件。

- 正在被 AI 修改的那一份文件才显示黄色提示或"正在更新预览"。
- 没有参与本轮编辑的文件不应被错误地标记为编辑中。

## 7. 草稿期间切换文件

当用户已经把选区添加到主输入框、但尚未发送时，这些选区与当前文件强绑定。

此时用户尝试切换到另一份文件，系统必须确认：

> 对话框有文档选区。如果切换查看新文件，当前选区将被清空。是否确认切换？

- 用户取消：留在当前文件，输入框内容保持不变。
- 用户确认：移除本轮待发送选区标签，其局部指令文字保留在输入框中作为普通文字，再切换文件。

本 Demo 不支持把不同文件的待发送选区混在同一轮草稿中。

## 8. 用户体验底线

1. 用户知道 AI 改的是哪里：每条局部请求都带文件和选区标签。
2. 用户可以先思考再发送：多个选区、局部指令和总要求能在一个输入框里混排、编辑和重组。
3. 用户不会因为点输入框或按钮而丢失刚选中的内容和正在填写的指令。
4. 用户知道当前文件是否正在被修改，也知道手动编辑可能带来的风险。
5. 用户查看其他文件时，不会看到与自己无关的"编辑中"状态。
6. 用户删除选区时，系统只移除这个选区，不悄悄删除用户写下的指令。

## 9. 本 Demo 的边界

- 支持：DOCX 文本选择、PPT 文字形状内选择、XLSX 连续单元格区域选择。
- 未支持：PDF 选区、图片/图表/形状等非文本对象选择、真实文件写入、真实 AI 执行、跨文件草稿。
- 待发送选区不产生文件侧高亮与批注。
- Demo 中的"编辑中"和"更新预览"是交互示意；接入实际能力后，必须以真实任务状态替换。

## 10. 设计规范与素材来源

- 所有新增界面遵循 KDesign 规范：组件、间距、颜色取 kd-skills 的 tokens 与组件文档；图标一律取自 KDicon-pro 图标库。
- "AI 编辑"按钮使用灵犀 icon（原型现有资产）。
- 悬浮工具栏容器用 KDesign Popover 规范，布局按产品侧提供的线上截图；确需手搓的部分，先与产品侧确认再实施。

## 附：当前原型基线

- 对话产出 docx / pptx / xlsx 卡片，点击在右侧分屏打开；多 tab 切换、关闭、全屏。
- 三组件编辑视图：文字纸张、演示（缩略图切页 + 演讲者备注沉底）、表格（公式栏 + 全屏网格 + sheet 页签）；均具备基本编辑能力。
- 顶部工具栏、状态栏、保存按钮（外观）已就绪。
